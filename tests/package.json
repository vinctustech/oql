{
  "name": "oql-tests",
  "type": "module",
  "private": true,
  "dependencies": {
    "@vinctus/oql": "file:../npm",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4"
  },
  "scripts": {
    "test": "tsx global-setup.ts && node --test --import tsx *.test.ts",
    "db:start": "docker compose up -d",
    "db:stop": "docker compose down"
  }
}
