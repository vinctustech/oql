services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: docker
    ports:
      - '5434:5432'
    volumes:
      - ../test/simple.sql:/docker-entrypoint-initdb.d/init.sql
