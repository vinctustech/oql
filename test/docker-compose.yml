postgres:
  image: postgres:13.3
  restart: always
  environment:
    - POSTGRES_USER=postgres
    - POSTGRES_PASSWORD=postgres
  ports:
    - '5432:5432'
  volumes:
    # copy the sql script to create tables
    - ./test/book.dm.sql:/docker-entrypoint-initdb.d/book.dm.sql
